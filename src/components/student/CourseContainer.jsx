// the component will handle fetching of the courses and mapping them, can use mock data or json database
import React from 'react'

export default function CourseContainer() {
  return (
    <div>CourseContainer</div>
  )
}
